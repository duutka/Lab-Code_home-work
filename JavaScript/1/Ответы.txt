1. Что такое переменная? И в чем ее разница со значением?
Переменная это хранилище данных в которое помещается значение. Мы можем изменить значение переменной столько раз, сколько захотим.
2. Что такое примитивный тип и неприметивный? В чем разница между ними? Приведите конкретный пример.
В примитивных типах значениями могут быть только простые значения(строка, число или что-то еще). В неприметивных типах хранят
колекции данных или более сложные конструкции.
Например:
let a = 4 - number(приметивный тип)
let arr = [1, 2, 3] - array(неприметивный тип, так как хранит коллекцию значений)
3. Создайте любой объект, добавьте в него новый ключ со значением, потом удалите любой существующий ключ.
Потом проитерируйте объект используя Object.entries, Object.values, Object.keys.
Пример в 3.js
4. Создайте любой массив, измените массив используя методы push, unshift, shift, pop. 
Как можно удалить элемент из массива по значение и по номер элемента в массиве, напишите пример?
Пример в 4.js
5. Что такое поверхностное и глубокое копирование? В чем разинца между ними? Приведите примеры
Поверхностное копирование это копирование всех свойств объекта без вложенности. Когда свойство объекта само является объектом, то
тогда нужно использовать глубокое копирование. Пример в 5.js